# DAY5-NOTE

## 面向对象

### 类

#### 继承

继承的核心机制：

语法：class 子类名(父类名):

单继承：一个子类只有一个父类

多继承：一个子类有多个父类，如class C(A, B):

方法继承：子类自动获得父类所有非私有方法

属性继承：子类获得父类所有非私有属性

方法重写：子类可重新定义父类方法，覆盖原有实现

super()调用：在子类中调用父类方法，保持继承链
